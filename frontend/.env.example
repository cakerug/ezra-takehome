# Base URL of the backend API. Copy this file to `.env` to override the built-in default
# (http://localhost:5265). Only needed if you run the backend on a different host/port.
VITE_API_BASE_URL=http://localhost:5265
